export type User = {
    id: string;
    name: string;
    email: string;
    password: string;
};

export type PlayableRace = {
    id: number;
    name: string;
};

export type PlayableClass = {
    id: number;
    name: string;
};